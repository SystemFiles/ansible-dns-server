;
; BIND data file for {{ domain_name }}
;
$TTL    604800
@       IN      SOA     {{ domain_name }}. admin.{{ domain_name }}. (
                              3         ; Serial
                         604800         ; Refresh
                          86400         ; Retry
                        2419200         ; Expire
                         604800 )       ; Negative Cache TTL
; name servers - NS records
        IN      NS      ns1.{{ domain_name }}.

; name servers - A records
ns1.{{ domain_name }}.        IN      A       127.0.0.1

; HOSTS - A records
{{ privary_host_name }}.{{ domain_name }}.  IN      A       {{ primary_host_address }}